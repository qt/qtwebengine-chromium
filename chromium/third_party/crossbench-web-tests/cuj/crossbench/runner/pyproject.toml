[tool.poetry]
description = "Runs web-test CUJs using crossbench."
authors = ["Kameron Lutes <kalutes@google.com>"]
readme = "README.md"
package-mode = false

[tool.poetry.dependencies]
python = ">=3.11,<3.12"
crossbench = { path = "../../../third_party/crossbench", develop = true }
pytype = "^2024.10.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
