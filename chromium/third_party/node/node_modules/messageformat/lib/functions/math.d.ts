import type { MessageFunctionContext } from './index.ts';
import { MessageNumber } from './number.ts';
/**
 * `math` accepts a numeric value as input and adds or subtracts an integer value from it
 *
 * @beta
 */
export declare function math(ctx: MessageFunctionContext, exprOpt: Record<string | symbol, unknown>, operand?: unknown): MessageNumber;
